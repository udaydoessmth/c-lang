#include<stdio.h>

int main(){
    int a,b;
    printf("enter two numbers: ");
    scanf("%d%d",&a,&b);
    if(a==b){
        printf("the given numbers %d and %d are equal",a,b);
    }
    else{
        printf("the given numbers are not equal");
    }
    return 0;
}
