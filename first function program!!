#include <stdio.h>
int sum(int a,int b)
{
    return a+b;
}
int main()
{
    int a,b,c;
    printf("enter a and b: ");
    scanf("%d%d",&a,&b);
    c=sum(a,b);
    printf("sum is %d",c);
    return 0;
}
